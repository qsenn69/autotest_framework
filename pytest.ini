[tool:pytest]
testpaths = tests
addopts =
    --strict-markers
    --tb=short
    --maxfail=1
    --disable-warnings
    --html=reports/report.html
    --self-contained-html
markers =
    smoke: smoke tests
    regression: regression tests
    slow: slow-running tests